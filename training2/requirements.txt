# 训练依赖 — 脚本自动安装，无需手动 pip install
# ── 注意 ──
# AMD 显卡需先手动安装 DirectML 版 PyTorch:
#   pip uninstall torch torchvision torchaudio -y
#   pip install torch-directml
# ──────────
modelscope
transformers
peft
datasets
accelerate
trl
sentencepiece
